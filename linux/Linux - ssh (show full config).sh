
# To get the current session's full SSH-configuration (at any given point in time), run the command:
ssh -G host;

# Use the following resource (or any other resource which exhaustively states all SSH-Config options)
https://www.ssh.com/ssh/config/
