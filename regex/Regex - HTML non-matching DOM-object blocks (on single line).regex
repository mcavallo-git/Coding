// ------------------------------------------------------------
// Regex - HTML non-matching DOM-object blocks (on single line)
// ------------------------------------------------------------

// Find lines with more opening DOM-blocks than closing DOM-blocks

// Find Regex:
^((?!</sub>).)*(<sub>((?!</sub>).)*){2}((?!</sub>).)*</sub>((?!</sub>).)*$

// Replace-with:
RESOLVE MANUALLY


// ------------------------------------------------------------

// Find lines with more closing DOM-blocks than opening DOM-blocks

// Find Regex:
^((?!<sub>).)*(<sub>((?!<sub>).)*){1}((?!<sub>).)*(</sub>((?!<sub>).)*){2,}$

// Replace-with:
RESOLVE MANUALLY


// ------------------------------------------------------------