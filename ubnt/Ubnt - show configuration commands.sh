#!/bin/bash

# Ubnt (ERLite-3)

# 'show configuration commands' reverse-engineers the current config and outputs what it would take to rebuild it (as a set of shell-commands)

show configuration commands

